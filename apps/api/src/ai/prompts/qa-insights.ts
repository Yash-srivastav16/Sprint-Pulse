// TODO: Re-enable external QA insights after the integration is moved to
// env-based config and typed ESM imports. The prototype is intentionally
// disabled so the API build does not depend on local QA scripts.
export {};
