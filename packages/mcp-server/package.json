{
  "name": "@sprintpulse/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Model Context Protocol server exposing SprintPulse risk + standup tools to any MCP-capable agent.",
  "main": "dist/index.js",
  "bin": {
    "sprintpulse-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "tsx": "^4.19.1",
    "typescript": "^5.9.3"
  }
}
